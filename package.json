{
  "name": "github-explorer",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack GitHub Explorer (React + Express, monorepo)",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm start --prefix server",
    "dev": "concurrently -n client,server -c green,blue \"npm run client\" \"npm run server\"",
    "test": "npm test --prefix client && npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
